#include <stdio.h>
int main() {
    char c;
    //enter a character
    //we can write print to show something to computer
    scanf("%c",&c);
    //in c characters are represented by ascii value when %d
    //ascii value
    printf("ASCII Value: %d",c);
    return 0;
}